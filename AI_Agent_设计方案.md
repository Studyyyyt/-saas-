# 口腔门诊 SaaS AI Agent 体系设计方案

## 一、设计目标

让 AI 覆盖门诊管理的**所有核心业务场景**：
- **查数据**：问一句就能查到统计结果
- **做分析**：自动发现异常、趋势、机会
- **生成内容**：病历、话术、方案一键生成
- **决策辅助**：排班、采购、预警智能建议

---

## 二、Agent 分类体系（按角色场景）

### 第一层：首页通用助手（1个）

| AgentKey | 名称 | 功能 | 适用角色 |
|---------|------|------|---------|
| `home-assistant` | AI 智能助手 | 通用问答、路由分发、跨模块综合查询 | 全员 |

**快捷指令示例**：
- "帮我分析本月经营情况" → 调用经营分析
- "张三患者最近什么情况" → 调用患者洞察
- "下周排班怎么安排合理" → 调用排班建议

---

### 第二层：经营管理层（4个）

| AgentKey | 名称 | 核心功能 | 输入 | 输出 | 适用角色 |
|---------|------|---------|------|------|---------|
| `business-analysis` | 经营分析 | 门诊运营总览：收入、患者量、预约率、医生绩效 | 日期范围 | 经营报告 + 趋势图数据 + 改进建议 | 管理员/老板 |
| `financial-analysis` | 财务分析 | 收支明细、科室利润、异常收费预警 | 时间范围 + 维度 | 财务报表 + 异常项标红 | 管理员/财务 |
| `appointment-analysis` | 预约分析 | 预约转化率、爽约率、时段热力图、医生负荷 | 日期范围 | 预约效率报告 + 优化建议 | 前台/管理员 |
| `marketing-analysis` | 营销分析 | 患者来源渠道 ROI、广告效果、获客成本 | 时间范围 + 渠道 | 营销效果报告 + 预算建议 | 运营/管理员 |

---

### 第三层：医疗业务层（5个）

| AgentKey | 名称 | 核心功能 | 输入 | 输出 | 适用角色 |
|---------|------|---------|------|------|---------|
| `medical-record-expand` | 病历扩写 | 根据关键词扩写完整病历 | 关键词/草稿 | 结构化病历 JSON | 医生 |
| `diagnosis-assist` | 诊断辅助 | 根据症状+检查给出鉴别诊断建议 | 症状描述 + 检查结果 | 诊断建议 + 鉴别要点 | 医生 |
| `treatment-plan-assist` | 治疗方案 | 根据诊断生成治疗方案+费用预估 | 诊断 + 患者情况 | 治疗方案 + 费用明细 + 周期 | 医生 |
| `patient-insight` | 患者洞察 | 单患者全景画像、治疗历史、风险标签 | 患者 ID/姓名 | 患者画像报告 + 风险提示 | 医生/护士 |
| `patient-followup` | 随访建议 | 根据治疗记录生成个性化随访计划 | 患者治疗记录 | 随访时间表 + 话术模板 | 医生/护士 |

---

### 第四层：运营服务层（4个）

| AgentKey | 名称 | 核心功能 | 输入 | 输出 | 适用角色 |
|---------|------|---------|------|------|---------|
| `followup-generate` | 回访生成 | 批量生成待回访患者列表和话术 | 时间范围 + 治疗类型 | 回访任务列表 + 话术 | 前台/客服 |
| `consultation-assist` | 咨询辅助 | 回答患者常见咨询问题、生成跟进建议 | 患者咨询内容 | 回复话术 + 跟进建议 | 前台/咨询 |
| `consultation-conversion` | 咨询转化 | 分析咨询→预约→到诊的转化漏斗 | 日期范围 | 转化漏斗报告 + 流失原因 | 管理员 |
| `patient-retention` | 留存分析 | 识别流失风险患者、生成召回策略 | 时间范围 | 流失预警名单 + 召回方案 | 运营/管理员 |

---

### 第五层：供应链层（3个）

| AgentKey | 名称 | 核心功能 | 输入 | 输出 | 适用角色 |
|---------|------|---------|------|------|---------|
| `material-inventory-assist` | 库存管理 | 库存预警、效期提醒、安全库存建议 | 当前库存数据 | 预警清单 + 采购建议 | 护士/库管 |
| `material-purchase-assist` | 采购建议 | 根据消耗趋势生成采购计划 | 历史消耗数据 + 预算 | 采购清单 + 供应商比价 | 管理员/采购 |
| `lab-statistics-analysis` | 加工分析 | 加工单统计、加工厂绩效、返工率分析 | 日期范围 | 加工效率报告 + 质量预警 | 护士/管理员 |

---

### 第六层：员工管理层（2个）

| AgentKey | 名称 | 核心功能 | 输入 | 输出 | 适用角色 |
|---------|------|---------|------|------|---------|
| `schedule-suggestion` | 排班建议 | 根据预约量+医生专长优化排班 | 历史预约 + 医生信息 | 推荐排班表 + 理由 | 管理员 |
| `doctor-performance` | 医生绩效 | 单个/全体医生的工作量、收入、患者满意度 | 日期范围 + 医生 | 绩效排名 + 改进建议 | 管理员 |

---

## 三、实施路线图（推荐优先级）

### 第一阶段（1-2周）：高频刚需
1. **病历扩写** `medical-record-expand` — 医生每天都在用
2. **经营分析** `business-analysis` — 管理员每天看首页
3. **患者洞察** `patient-insight` — 点击患者即可触发

### 第二阶段（2-3周）：效率提升
4. **回访生成** `followup-generate` — 减少人工筛选
5. **库存预警** `material-inventory-assist` — 避免断货/过期
6. **咨询辅助** `consultation-assist` — 提升前台响应速度

### 第三阶段（3-4周）：深度分析
7. **财务分析** `financial-analysis` — 精细化财务管理
8. **预约分析** `appointment-analysis` — 优化资源利用
9. **营销分析** `marketing-analysis` — 降本增效

### 第四阶段（按需）：精细化运营
10. **留存分析** `patient-retention`
11. **医生绩效** `doctor-performance`
12. **排班建议** `schedule-suggestion`

---

## 四、技术对接规范

### 1. 前端配置（AI 智能中心）

每个 Agent 配置时填写：
- **AgentKey**：使用上表中的 key
- **名称**：如"经营分析"
- **Webhook 地址**：n8n 工作流地址或 mock 地址
- **快捷指令**：预设 3-5 个常用问法

### 2. 后端协议（统一格式）

前端发送的标准请求体：
```json
{
  "message": "用户输入的文本",
  "account_id": "1",
  "account_name": "管理员",
  "session_id": "xxx",
  "clinic_id": "1"
}
```

Webhook 返回的标准格式：
```json
{
  "code": "200",
  "msg": "success",
  "data": {
    // 文本回复：前端直接显示
    "reply": "本月门诊收入...",
    
    // 或结构化数据：前端走 JSON 卡片展示
    "chief_complaint": "...",
    "diagnosis": "..."
  }
}
```

### 3. 工作流设计建议（n8n）

每个工作流的标准节点：
1. **Webhook 触发节点** — 接收后端转发请求
2. **数据查询节点** — 调用系统 API 获取原始数据（HTTP Request）
3. **AI 处理节点** — 把数据 + 用户问题发给大模型
4. **格式化输出节点** — 按标准 JSON 格式返回

**示例：经营分析工作流**
```
Webhook → HTTP Request(查财务API) → HTTP Request(查预约API) 
  → AI Agent(分析数据) → 格式化JSON输出
```

---

## 五、 mock 测试数据建议

为每个 Agent 写 mock 时，按以下模板：

```python
# 经营分析 mock
@app.route("/ai-business", methods=["POST"])
def ai_business():
    return jsonify({
        "code": "200",
        "msg": "success",
        "data": {
            "reply": "本月经营概况：\n\n"
                     "📊 总收入：¥128,500（环比 +12%）\n"
                     "👥 新患者：45人（环比 +8%）\n"
                     "📅 预约到诊率：87%（目标 90%）\n"
                     "⚠️ 异常：周五下午预约量偏低，建议增加推广"
        }
    })
```

---

## 六、字段中文映射（供前端 renderJsonCard 扩展）

如果你返回结构化 JSON，建议字段命名：

| 英文字段 | 中文标签 | 适用场景 |
|---------|---------|---------|
| `reply` / `content` | 回复内容 | 通用文本回复 |
| `summary` | 摘要 | 分析报告 |
| `suggestion` | 建议 | 决策辅助 |
| `warning` | 预警 | 异常提醒 |
| `metrics` | 核心指标 | 数据看板 |
| `trend` | 趋势 | 趋势分析 |
| `comparison` | 对比 | 同比环比 |

如需添加更多字段映射，修改 `HomeView.vue` 中 `renderJsonCard` 的 `fieldOrder` 数组即可。

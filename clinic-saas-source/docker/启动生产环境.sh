#!/bin/bash
# ============================================================
# 快捷脚本：一键启动生产环境
# ============================================================

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR" || exit 1

exec ./环境切换.sh prod

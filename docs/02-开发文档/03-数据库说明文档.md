# 口腔门诊 SaaS 数据库说明文档

> 数据库名：`clinic_system`  
> 字符集：`utf8mb4`（支持 Emoji 和中文生僻字）  
> 存储引擎：`InnoDB`  
> 表总数：53 张（不含 Flyway 迁移历史表）  
> 导出日期：2026-05-06

---

## 一、初始化方式

### 方式一：Docker MySQL 首次启动自动导入
将 `schema.sql` 和 `seed.sql` 放入 MySQL 容器的初始化目录：
```yaml
# docker-compose.yml 中 mysql 服务配置
services:
  mysql:
    image: mysql:8.0
    volumes:
      - ./requirements/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./requirements/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql
      - mysql_data:/var/lib/mysql
```
MySQL 首次启动时会按文件名顺序（01 → 02）自动执行这两个 SQL 文件。

### 方式二：手动导入
```bash
# 1. 创建数据库
mysql -u root -p -e "CREATE DATABASE clinic_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# 2. 导入结构
mysql -u root -p clinic_system < requirements/schema.sql

# 3. 导入基础数据
mysql -u root -p clinic_system < requirements/seed.sql

# 4. 启动后端，Flyway 会自动执行 V33+ 的新迁移脚本
```

### 方式三：由后端 Flyway 自动管理（推荐开发环境）
- 不手动导入 `schema.sql`
- 启动后端服务时，Flyway 会自动按 V1 → V32 的顺序执行所有迁移脚本创建表结构
- 然后执行 `seed.sql` 插入基础数据
- 后续新功能通过新增 `V33__xxx.sql` / `V34__xxx.java` 进行迭代

---

## 二、模块划分与表清单

### 2.1 患者管理模块（核心主数据）

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `patients` | 患者主表 | `id`, `name`, `gender`, `age`, `phone`, `address`, `customer_source`, `relation_type`, `related_patient_id` |
| `patient_followup` | 患者随访记录 | `patient_id`, `followup_type`, `followup_date`, `content`, `next_followup_date` |
| `patient_risk_tag` | 患者风险标签 | `patient_id`, `tag_name`, `risk_level` |
| `patient_timeline` | 患者事件时间轴 | `patient_id`, `event_type`, `event_date`, `event_description` |
| `patient_images` | 患者影像资料 | `patient_id`, `image_url`, `image_type`, `upload_date` |
| `patient_consent` | 患者知情同意书签署记录 | `patient_id`, `consent_template_id`, `signed_at` |
| `patient_custom_group` | 患者自定义分组 | `group_name`, `group_type`, `created_by` |
| `patient_custom_group_member` | 分组-患者关联 | `group_id`, `patient_id` |
| `patient_insight_summary` | AI 患者洞察摘要 | `patient_id`, `insight_type`, `summary_content` |
| `patient_referral_records` | 患者转诊记录 | `referrer_patient_id`, `referred_patient_id`, `referral_date` |
| `patient_wechat_bind_scene` | 患者微信绑定场景 | `patient_id`, `wechat_openid`, `bind_scene` |

### 2.2 预约管理模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `appointment` | 预约记录 | `id`, `patient_id`, `patient_name`, `appointment_date`, `appointment_time`, `doctor_account_id`, `status`, `cancel_reason` |
| `doctors` | 医生档案 | `id`, `name`, `title`, `specialty`, `schedule` |
| `doctor_home_reminder_dismissal` | 医生首页提醒忽略 | `doctor_account_id`, `reminder_type`, `dismissed_at` |

### 2.3 病历与治疗模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `medical_records` | 病历主表 | `id`, `patient_id`, `patient_name`, `doctor_account_id`, `visit_date`, `chief_complaint`, `diagnosis`, `treatment_plan`, `tooth_positions` |
| `medical_record_operations` | 病历诊疗操作项 | `medical_record_id`, `operation_name`, `tooth_position`, `fee` |
| `medical_record_template` | 病历模板 | `template_name`, `category`, `content_json` |
| `treatment` | 处置记录 | `id`, `patient_id`, `patient_name`, `treatment_date`, `treatment_content`, `treatment_fee`, `doctor_account_id`, `medical_record_id` |
| `treatment_plans` | 治疗计划 | `patient_id`, `plan_content`, `planned_date`, `status` |
| `treatment_catalog` | 治疗项目目录 | `id`, `project_name`, `category`, `unit_price`, `unit` |
| `treatment_operations` | 治疗手术记录 | `treatment_id`, `operation_name`, `doctor_id`, `assistant_id`, `operation_date` |
| `treatment_operation_allocations` | 手术绩效分配 | `operation_id`, `doctor_id`, `allocation_ratio`, `allocation_amount` |
| `treatment_projects` | 治疗项目（关联关系） | `project_name`, `category_id`, `price` |
| `treatment_project_categories` | 治疗项目分类 | `category_name`, `parent_id` |
| `project_operation_relations` | 项目-操作关联 | `project_id`, `operation_id` |
| `consent_template` | 知情同意书模板 | `template_name`, `content`, `applicable_projects` |

### 2.4 财务模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `finances` | 财务收支记录 | `id`, `patient_id`, `transaction_type`, `amount`, `payment_method`, `transaction_date`, `doctor_id` |
| `payment_channel` | 收款渠道配置 | `channel_name`, `status`, `sort_order` |
| `advertising_spending` | 广告投放支出 | `platform`, `campaign_name`, `amount`, `start_date`, `end_date` |
| `purchases` | 采购记录 | `supplier`, `total_amount`, `purchase_date`, `status` |

### 2.5 耗材/库存/义齿加工模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `inventory` | 库存主表 | `item_name`, `category`, `quantity`, `safety_stock`, `unit_price` |
| `materials` | 耗材档案 | `id`, `name`, `category_id`, `specification`, `unit_price`, `stock_quantity` |
| `material_categories` | 耗材分类 | `name`, `parent_id`, `sort_order` |
| `material_purchases` | 耗材采购单 | `purchase_no`, `supplier`, `total_amount`, `status` |
| `material_purchase_items` | 采购单明细 | `purchase_id`, `material_id`, `quantity`, `unit_price` |
| `lab_factories` | 义齿加工厂 | `name`, `contact`, `address`, `status` |
| `lab_factory_products` | 加工厂产品目录 | `factory_id`, `product_name`, `price` |
| `lab_orders` | 技工订单 | `patient_id`, `factory_id`, `product_id`, `order_date`, `delivery_date`, `status` |
| `lab_bills` | 技工账单 | `factory_id`, `bill_month`, `total_amount`, `status` |
| `lab_bill_items` | 账单明细 | `bill_id`, `order_id`, `amount` |
| `lab_bill_templates` | 账单导入模板 | `factory_id`, `template_name`, `column_mapping` |
| `lab_bill_unmatched_orders` | 账单未匹配订单 | `bill_id`, `order_info`, `reason` |

### 2.6 咨询与营销模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `consultation_records` | 咨询记录 | `patient_name`, `phone`, `consultation_date`, `consultation_channel`, `customer_source`, `referrer` |

### 2.7 医保模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `insurance_config` | 医保配置 | `insurance_type`, `config_key`, `config_value` |
| `insurance_patient_profile` | 医保患者档案 | `patient_id`, `insurance_no`, `insurance_type`, `balance` |
| `insurance_settlement` | 医保结算记录 | `patient_id`, `settlement_date`, `total_amount`, `insurance_amount`, `self_amount` |
| `insurance_operation_log` | 医保操作日志 | `operation_type`, `patient_id`, `request_data`, `response_data` |

### 2.8 AI 经营分析模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `business_daily_analysis` | 每日经营分析 | `analysis_date`, `headline`, `summary`, `risks`, `opportunities`, `actions`, `operating_score` |
| `business_period_report` | 周期报告（周/月） | `report_type`, `period_start`, `period_end`, `content_json` |
| `business_alert_log` | 经营异常告警 | `alert_type`, `metric_name`, `metric_value`, `threshold`, `alert_level` |

### 2.9 微信生态模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `portal_access_token` | 患者门户访问令牌 | `patient_id`, `token`, `expire_at` |

### 2.10 权限与系统模块

| 表名 | 说明 | 关键字段 |
|------|------|---------|
| `users` | 系统用户 | `id`, `username`, `password`, `name`, `role` |
| `role_menu_permissions` | 角色菜单权限 | `role`, `menu_code`, `permission_type` |

---

## 三、核心数据关系

```
patients (患者主表)
├── 1:N → appointment (预约)
├── 1:N → medical_records (病历)
├── 1:N → treatment (处置)
├── 1:N → finances (财务)
├── 1:N → patient_followup (随访)
├── 1:N → patient_timeline (时间轴)
├── 1:N → patient_images (影像)
├── 1:N → patient_consent (知情同意)
├── 1:N → patient_risk_tag (风险标签)
├── 1:N → lab_orders (技工订单)
├── 1:1 → patient_insight_summary (AI 洞察)
└── 1:N → patient_referral_records (转诊)

medical_records (病历)
├── 1:N → medical_record_operations (诊疗操作项)
└── 1:N → treatment (处置，通过 medical_record_id 关联)

treatment_catalog (治疗项目)
└── 1:N → treatment (处置，通过项目名称/ID 关联)

material_categories (耗材分类)
└── 1:N → materials (耗材档案)

material_purchases (采购单)
└── 1:N → material_purchase_items (采购明细)

lab_factories (加工厂)
├── 1:N → lab_factory_products (产品目录)
├── 1:N → lab_orders (技工订单)
└── 1:N → lab_bills (月度账单)

lab_bills (账单)
├── 1:N → lab_bill_items (账单明细)
└── 1:N → lab_bill_unmatched_orders (未匹配订单)
```

---

## 四、关键设计说明

### 4.1 关联方式
- **无数据库级外键约束**：项目采用逻辑外键（`patient_id`），未设置 `FOREIGN KEY` 约束。
- **冗余存储 `patient_name`**：`appointment`、`medical_records`、`treatment`、`finances` 等子表同时存储 `patient_id` 和 `patient_name`，用于避免频繁 JOIN 查询。
- **风险**：患者改名后，子表中的 `patient_name` 不会自动同步（已在阶段一需求中规划修复）。

### 4.2 时间戳规范
- 所有业务表均包含 `created_at` 和 `updated_at` 字段：
  - `created_at`：`DEFAULT CURRENT_TIMESTAMP`
  - `updated_at`：`DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP`

### 4.3 主键策略
- 所有表使用 `BIGINT AUTO_INCREMENT` 作为主键（`users` 表为 `INT`，历史原因）。

### 4.4 索引策略
- 主键均为 `PRIMARY KEY`
- 高频查询字段已建立普通索引（如 `patient_id`、`appointment_date`、`status` 等）
- 部分表建立了联合索引（如 `advertising_spending` 的 `(platform, start_date)`）

### 4.5 敏感数据
- `users.password`：当前为明文存储（`admin123`），**强烈建议**后续升级为 BCrypt 加密
- `patients.phone`：患者手机号未加密存储，生产环境建议脱敏或加密

---

## 五、文件清单

| 文件 | 说明 | 使用场景 |
|------|------|---------|
| `schema.sql` | 完整表结构（53 张表 + 索引） | Docker 初始化 / 手动建库 |
| `seed.sql` | 基础数据（admin 用户 + 5 个材料分类 + 4 个支付渠道） | 系统首次启动后必需 |

---

## 六、后续扩展预留

以下表尚未在迁移脚本中定义，为后续阶段预留：

| 预留表名 | 说明 | 对应阶段 |
|---------|------|---------|
| `external_agent_config` | 外部 AI Agent 配置 | 阶段四 |

新增表时，建议通过 Flyway 迁移脚本（`V33__ExternalAgentConfig.sql`）执行，保持版本化管理。

---

## 七、AI相关表结构（新架构）

> 本章节说明 AI 模块表结构的最新变更，适用于新架构下的外部工作流集成方案。

### 7.1 `ai_agent_config` 表变更说明

#### 新增字段

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `endpoint_url` | VARCHAR(500) | 外部工作流端点地址，系统通过此 URL 调用外部 AI 服务 |
| `auth_type` | VARCHAR(50) | 认证类型，如 `none`、`bearer`、`api_key` |
| `auth_token` | VARCHAR(500) | 认证令牌，根据 `auth_type` 在请求中使用 |
| `request_template` | TEXT | 请求体模板，用于构造发送到外部端点的请求格式 |
| `response_type` | VARCHAR(50) | 响应类型，`sse`（流式）或 `json`（一次性返回） |
| `timeout_seconds` | INT | 请求超时时间，单位秒，默认 30 |

#### 废弃字段

| 字段名 | 废弃原因 |
|--------|---------|
| `system_prompt` | 新架构下系统不再直接调用 AI 模型，提示词逻辑由外部工作流平台维护 |
| `enabled_tools` | 工具调用能力由外部工作流平台实现，本系统不再维护工具配置 |

#### 表用途变更

- **旧用途**：配置 AI 智能体的系统提示词、启用工具、模型参数等，由系统直接调用大语言模型。
- **新用途**：配置外部工作流端点（如 n8n、Dify、Coze 等），所有 AI 逻辑通过外部工作流平台完成。本系统仅作为代理层，将用户请求转发至配置的外部端点，并将响应返回给前端。

### 7.2 架构说明

新架构下，系统的 AI 能力不再依赖内置的模型提供商配置和直接模型调用，而是通过以下流程实现：

1. 管理员在「AI 助手配置」页面配置外部工作流端点（`ai_agent_config` 表）。
2. 前端通过统一 AI 代理接口（`POST /api/ai/proxy/{agentKey}`）发起请求。
3. 后端根据 `agentKey` 查找对应的 `ai_agent_config` 配置。
4. 后端按照 `request_template` 构造请求，发送至 `endpoint_url`。
5. 外部工作流平台执行完整的 AI 逻辑（包括提示词工程、工具调用、多轮对话等）。
6. 后端将外部平台的响应（SSE 流式或 JSON）透传回前端。

此架构的优势：
- **灵活性**：可在不修改系统代码的情况下，切换或升级 AI 工作流。
- **可维护性**：提示词和工具逻辑由外部平台管理，版本控制更方便。
- **扩展性**：支持对接任意支持 HTTP/SSE 的外部工作流平台。
